import React from 'react';

const LoadingSpinner = () => (
  <div className="text-center">
    <div className="spinner-border text-primary" role="status">
      <span className="sr-only">Leap Event Technology</span>
    </div>
  </div>
);

export default LoadingSpinner;